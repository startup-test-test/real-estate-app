# 未使用ソースコード一覧

調査日: 2026-02-18（第2回更新）

ooya-dx_2026 プロジェクト内で現在使用されていないソースコードの一覧です。
リファクタリング時の削除候補としてご活用ください。

---

## 1. 未使用ページ/ルート（4件）

| ファイル | 状況 | 判定 |
|---------|------|------|
| `app/tools/template-demo/` | デモページ。`sitemap.ts` で除外済み、`robots: noindex` 設定 | 削除OK |
| `app/test-error/` | エラーテストページ。コード内に「本番では削除推奨」と明記。`robots.ts` で disallow 設定 | 削除OK |
| `app/company/layout-sample/` | レイアウトサンプル。参照ゼロ | 削除OK |
| `app/api/simulations/test/` | テスト用 API エンドポイント。自己参照のみ | 削除OK |

**削除後の修正:**
- `robots.ts`: `/test-error` の disallow 行を削除
- `sitemap.ts`: `template-demo` の除外フィルター行を削除（残しても害なし）

---

## 2. 未使用カスタムフック（6件）

`hooks/index.ts` でコメントアウト済み（Supabase → Neon Auth 移行で無効化）

| ファイル | 備考 | 判定 |
|---------|------|------|
| `hooks/useSupabaseAuth.ts` | 旧 Supabase 認証。`useApiCall.ts` が import しているが useApiCall 自体も未使用 | 削除OK |
| `hooks/useSupabaseData.ts` | 旧 Supabase データ取得。コメントアウト参照のみ | 削除OK |
| `hooks/useUsageStatus.ts` | 使用状況フック。全参照がコメントアウト済み | 削除OK |
| `hooks/useApiCall.ts` | API 呼び出し汎用フック。index.ts でもコメントアウト | 削除OK |
| `hooks/useImageUpload.ts` | 画像アップロード。`components/simulator/ImageUpload.tsx` が import しているが、ImageUpload.tsx 自体がどこからもインポートされていない | 削除OK（ImageUpload.tsx と一緒に） |
| `hooks/useSimulationState.ts` | シミュレーション状態管理。`hooks/index.ts` から export されているが、その export を使っているファイルなし | 削除OK |

**一緒に削除:**
- `components/simulator/ImageUpload.tsx`（useImageUpload の唯一の利用元だが、ImageUpload 自体が未使用）

---

## 3. 未使用 UI コンポーネント（6件）

`components/shared/` 配下のコンポーネントでどこからもインポートされていないもの。

| ファイル | コンポーネント名 | 判定 |
|---------|----------------|------|
| `components/shared/ErrorBoundary.tsx` | ErrorBoundary | 削除OK |
| `components/shared/CancelSubscriptionModal.tsx` | CancelSubscriptionModal | 削除OK |
| `components/shared/SectionHeader.tsx` | SectionHeader | 削除OK |
| `components/shared/LoadingSpinner.tsx` | LoadingSpinner | 削除OK |
| `components/HelpButton.tsx` | HelpButton | 削除OK |
| `components/PWAInstallBanner.tsx` | PWAInstallBanner | 削除OK |

---

## 4. 未使用 calculators UI コンポーネント（10件）

`components/calculators/ui/` 配下のコンポーネントが `index.ts` でエクスポートされているが、実際にはどこからも使われていない。各計算ツールページでは独自のインラインUIを構築している。

| ファイル | コンポーネント名 |
|---------|----------------|
| `components/calculators/ui/CalculatorContainer.tsx` | CalculatorContainer |
| `components/calculators/ui/CalculatorTitle.tsx` | CalculatorTitle |
| `components/calculators/ui/CalculatorInputField.tsx` | CalculatorInputField |
| `components/calculators/ui/CalculatorResultBox.tsx` | CalculatorResultBox |
| `components/calculators/ui/CalculatorResultRow.tsx` | CalculatorResultRow |
| `components/calculators/ui/CalculatorMainResult.tsx` | CalculatorMainResult |
| `components/calculators/ui/CalculatorFormula.tsx` | CalculatorFormula |
| `components/calculators/ui/CalculatorCopyButton.tsx` | CalculatorCopyButton |
| `components/calculators/ui/CalculatorWarning.tsx` | CalculatorWarning |
| `components/calculators/ui/CalculatorRelatedLinks.tsx` | CalculatorRelatedLinks |

---

## 5. 未使用ユーティリティ・テンプレート（4件）

| ファイル | 状況 | 判定 |
|---------|------|------|
| `lib/calculators/simpleCashflow.ts` | どこからもインポートされていない | 削除OK |
| `lib/calculators/_template.ts` | 新規計算ロジック作成時のテンプレート。参照ゼロ | 削除OK |
| `lib/utils/errorHandler.test.ts` | ユニットテスト（Playwright 使用中で未実行） | 削除OK |
| `lib/utils/securityValidation.test.ts` | 同上 | 削除OK |
| `lib/utils/securityUtils.test.ts` | 同上 | 削除OK |

---

## 6. 未使用環境変数ファイル（2件）

| ファイル | 状況 | 判定 |
|---------|------|------|
| `.env.neon.example` | Neon 環境変数の参考ファイル | 削除OK |
| `.env.supabase.example` | 旧 Supabase 環境変数の参考ファイル | 削除OK |

---

## 7. 開発用テンプレート（参照用に残す候補）

| ファイル | 状況 | 備考 |
|---------|------|------|
| `app/tools/_template/page.tsx` | 新規ツール作成時の参照用テンプレート | `_` 始まりのため Next.js ルーティング対象外 |
| `app/tools/_template/ToolNameCalculator.tsx` | 同上 | 同上 |
| `app/tools/_template/README.md` | 同上 | 同上 |
| `app/tools/components/page.tsx` | コンポーネントショーケース（noindex 設定済） | 開発者用 |

---

## 8. 未使用画像・動画アセット（public/）

### 未使用画像

| ファイル | 状況 |
|---------|------|
| `public/img/people_4.png` | people_1〜3のみ使用中 |
| `public/img/tokuchou01.png` | .jpg版のみ使用中 |
| `public/img/tokuchou02.jpg` | 未参照 |
| `public/img/tokuchou02.png` | 未参照 |
| `public/img/tokuchou03.jpg` | 未参照 |
| `public/img/tokuchou03.png` | 未参照 |
| `public/img/logo_startup_marketing_white.png` | 白版未使用（通常版のみ使用中） |

### 未使用動画

| ファイル | 状況 |
|---------|------|
| `public/img/tokuchou_01_old.mp4` | 旧バージョン |
| `public/img/tokuchou_01_ver2.mov` | 旧バージョン |
| `public/img/tokuchou_02.mov` | 未参照 |
| `public/img/tokuchou_02.mp4` | 未参照 |
| `public/img/tokuchou_03.mov` | 未参照 |
| `public/img/tokuchou_03.mp4` | 未参照 |
| `public/img/douga_250827_1.mov` | 未参照 |
| `public/img/douga_250827_1.mp4` | 未参照 |

### 未使用サムネイル

| ファイル | 状況 |
|---------|------|
| `public/images/media/thumbnails/` 配下全体 | コードから未参照 |

---

## 整理の優先度

| 優先度 | 対象 | アクション | 理由 |
|--------|------|-----------|------|
| **高** | 未使用ページ/ルート（4件） | 削除 + robots.ts/sitemap.ts 修正 | テスト/デモページが公開される可能性 |
| **高** | 未使用動画・画像（15+ファイル） | 削除 | リポジトリサイズ削減に大きく貢献 |
| **中** | 未使用フック（6件 + ImageUpload.tsx） | 削除 | 旧 Supabase コード。Neon Auth 移行済みで不要 |
| **中** | 未使用コンポーネント（6件） | 削除 | コードの見通しが良くなる |
| **中** | calculators/ui コンポーネント（10件） | 今後使う予定がなければ削除 | 各ツールが独自 UI で実装済み |
| **低** | ユーティリティ・テストファイル（5件） | 削除 | 完全に未使用 |
| **低** | 環境変数 example ファイル（2件） | 削除 | 現在の設定は .env.local で管理 |
| **保留** | 開発用テンプレート（セクション7） | 残すか判断 | 新規ツール作成時に参考になる可能性 |

---

## 補足

- 上記以外の `components/` 配下のコンポーネントはすべて正常に使用されていることを確認済み
- `package.json` の依存パッケージについては本調査の対象外（別途調査が必要）
- `_template` プレフィックスのファイルは Next.js のルーティング対象外（`_` 始まりのフォルダは無視される）
- 本日実施済みのクリーンアップ: CLAUDE.md ゴミファイル 443個 + 空ディレクトリ 185個を削除
