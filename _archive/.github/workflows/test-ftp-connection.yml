name: Test FTP Connection

on:
  workflow_dispatch:

jobs:
  test-ftp:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Create test file
      run: |
        mkdir -p test-upload
        echo "FTP Connection Test - $(date)" > test-upload/test.txt
    
    - name: Test FTP Upload
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ${{ secrets.FTP_HOST }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        port: ${{ secrets.FTP_PORT }}
        local-dir: ./test-upload/
        server-dir: /public_html/dev/test/
        dangerous-clean-slate: false
    
    - name: Debug Secrets
      run: |
        echo "FTP_HOST is set: ${{ secrets.FTP_HOST != '' }}"
        echo "FTP_USERNAME is set: ${{ secrets.FTP_USERNAME != '' }}"
        echo "FTP_PASSWORD is set: ${{ secrets.FTP_PASSWORD != '' }}"
        echo "FTP_PORT is set: ${{ secrets.FTP_PORT != '' }}"